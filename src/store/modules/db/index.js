import { getUserData, saveUserRequest, updateUserRequest, getRequests } from './actions'

export default {
    actions: {
        getUserData,
        saveUserRequest,
        updateUserRequest,
        getRequests
    }
}